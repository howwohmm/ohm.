import { HeroSection } from '../components/HeroSection';

const Index = () => (
  <div style={{ height: '100svh', background: 'var(--bg)', overflow: 'hidden' }}>
    <HeroSection />
  </div>
);

export default Index;
